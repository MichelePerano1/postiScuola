from django import forms

class MyForm(forms.Form):
    input1 = forms.CharField(label="Posto 1", max_length=100 , widget=forms.TextInput(
            attrs={
                'class': 'formBox',  # This adds the class
                'placeholder': 'Cognome'  # This adds the placeholder
            }))
    input2 = forms.CharField(label="Posto 2", max_length=100,widget=forms.TextInput(
            attrs={
                'class': 'formBox',  # This adds the class
                'placeholder': 'Cognome'  # This adds the placeholder
            }))
    input3 = forms.CharField(label="Posto 3", max_length=100,widget=forms.TextInput(
            attrs={
                'class': 'formBox',  # This adds the class
                'placeholder': 'Cognome'  # This adds the placeholder
            }))

    input4 = forms.CharField(label="Posto 4", max_length=100,widget=forms.TextInput(
            attrs={
                'class': 'formBox',  # This adds the class
                'placeholder': 'Cognome'  # This adds the placeholder
            }))
    input5 = forms.CharField(label="Posto 5", max_length=100,widget=forms.TextInput(
            attrs={
                'class': 'formBox',  # This adds the class
                'placeholder': 'Cognome'  # This adds the placeholder
            }))
    
    input6 = forms.CharField(label="Posto 6", max_length=100,widget=forms.TextInput(
            attrs={
                'class': 'formBox',  # This adds the class
                'placeholder': 'Cognome'  # This adds the placeholder
            }))
    input7 = forms.CharField(label="Posto 7", max_length=100,widget=forms.TextInput(
            attrs={
                'class': 'formBox',  # This adds the class
                'placeholder': 'Cognome'  # This adds the placeholder
            }))
    
    
    input8 = forms.CharField(label="Posto 8", max_length=100,widget=forms.TextInput(
            attrs={
                'class': 'formBox',  # This adds the class
                'placeholder': 'Cognome'  # This adds the placeholder
            }))
    input9 = forms.CharField(label="Posto 9", max_length=100,widget=forms.TextInput(
            attrs={
                'class': 'formBox',  # This adds the class
                'placeholder': 'Cognome'  # This adds the placeholder
            }))
    input10 = forms.CharField(label="Posto 10", max_length=100,widget=forms.TextInput(
            attrs={
                'class': 'formBox',  # This adds the class
                'placeholder': 'Cognome'  # This adds the placeholder
            }))
    
    input11 = forms.CharField(label="Posto 11", max_length=100,widget=forms.TextInput(
            attrs={
                'class': 'formBox',  # This adds the class
                'placeholder': 'Cognome'  # This adds the placeholder
            }))
    input12 = forms.CharField(label="Posto 12", max_length=100,widget=forms.TextInput(
            attrs={
                'class': 'formBox',  # This adds the class
                'placeholder': 'Cognome'  # This adds the placeholder
            }))
    
    input13 = forms.CharField(label="Posto 13", max_length=100,widget=forms.TextInput(
            attrs={
                'class': 'formBox',  # This adds the class
                'placeholder': 'Cognome'  # This adds the placeholder
            }))
    input14 = forms.CharField(label="Posto 14", max_length=100,widget=forms.TextInput(
            attrs={
                'class': 'formBox',  # This adds the class
                'placeholder': 'Cognome'  # This adds the placeholder
            }))
    
    
    input15 = forms.CharField(label="Posto 15", max_length=100,widget=forms.TextInput(
            attrs={
                'class': 'formBox',  # This adds the class
                'placeholder': 'Cognome'  # This adds the placeholder
            }))
    input16 = forms.CharField(label="Posto 16", max_length=100,widget=forms.TextInput(
            attrs={
                'class': 'formBox',  # This adds the class
                'placeholder': 'Cognome'  # This adds the placeholder
            }))
    input17 = forms.CharField(label="Posto 17", max_length=100,widget=forms.TextInput(
            attrs={
                'class': 'formBox',  # This adds the class
                'placeholder': 'Cognome'  # This adds the placeholder
            }))
    
    input18 = forms.CharField(label="Posto 18", max_length=100,widget=forms.TextInput(
            attrs={
                'class': 'formBox',  # This adds the class
                'placeholder': 'Cognome'  # This adds the placeholder
            }))
    input19 = forms.CharField(label="Posto 19", max_length=100,widget=forms.TextInput(
            attrs={
                'class': 'formBox',  # This adds the class
                'placeholder': 'Cognome'  # This adds the placeholder
            }))
    
    input20 = forms.CharField(label="Posto 20", max_length=100,widget=forms.TextInput(
            attrs={
                'class': 'formBox',  # This adds the class
                'placeholder': 'Cognome'  # This adds the placeholder
            }))
    input21 = forms.CharField(label="Posto 21", max_length=100,widget=forms.TextInput(
            attrs={
                'class': 'formBox',  # This adds the class
                'placeholder': 'Cognome'  # This adds the placeholder
            }))
